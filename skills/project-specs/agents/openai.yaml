interface:
  display_name: "Project Specs"
  short_description: "Turn a brainstorm into a living product spec, full roadmap, and current-version tasks."
  default_prompt: "Use $project-specs to capture the desired end state, full version roadmap, and current implementation tasks, then stop before building."
